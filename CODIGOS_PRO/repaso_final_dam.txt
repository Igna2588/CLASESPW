try{
	bloque que puede fallas
} catch(){
	Ejecucion del fallo
} finally{
	ejecutado si o si
}

public void metodoPosibleError() throws IOException{
	llamada fallo
}

throw new Exception()

Integer[] numeros = new Integer[3] -> [7,7,7] -> numeros[0].algo
int[] numeros = new int[3] -> [0,0,0] -> numeros[0]

Boolean[] acuertos = new Boolean[5] -> [null, null, null, null, null]
boolean[] acuertos = new boolean[5] -> [false,false,false,false,false]

int[] numeros = new int[] -> fallo
int[] numeros = new int[5]{4,5,6,8,9} -> fallo. O defines elementos o defines cantidad

int[] numeros = new int[5]
int[] numeros = new int[]{4,5,6,8,9,1}

array.length en un array -> 

ArrayList -> new ArrayList<Alumnos>
	lista.add(new Alumno("Alumno1","Apellido1"));
	lista.add(new Alumno("Alumno2","Apellido1"));
	lista.add(new Alumno("Alumno3","Apellido1"));
	lista.add(new Alumno("Alumno4","Apellido1"));

	lista.get(0) -> Alumno

HashMap -> new HashMap<String,Alumno>()
	
	lista.put("2234A",new Alumno("Nombre2","Apellido1"))
	lista.put("3234A",new Alumno("Nombre3","Apellido1"))
	lista.put("1234A",new Alumno("Nombre4","Apellido1"))
	lista.put("1234A",new Alumno("Nombre1","Apellido1"))
	lista.get("1234A") -> Alumno


Flujos
	texto plano
		File -> Fichero apunta la accion
			Lecturas	File f = new File(ruta)
							-> FileReader (int a int) fr = new FileReader(f) fr.read() -> ASCII
							-> BufferedReader (string a string) br = new BufferedReader(fr) -> br.readLine() -> string
			Escrituras  File f = new File(ruta)
							-> FileWriter (int a int) fw = new FileWriter(f) fr.write("123") o fr.write(123) -> ASCII
							-> BufferedWriter o PrintWriter (string con salto) 
								pw = new PrintWriter(fw) -> pw.println("asdasdasd") -> string con salto de linea
								bw = new BufferedWriter(fw) -> bw.writeLine("asdasdasd") y ademas bw.newLine() -> string con salto de linea

							importantisimo en los flujos de datos -> cerrar el flujo -> .close() -> en el finally para que se haga SIEMPRE
	
	objetos -> Object
		File -> Fichero apunta la accion
			Input (Lecturas)
				ObjectInputStream ois -> FileInputStream -> File -> ois.read() -> lo casteo - (Alumno)ois.read()
			Output (Escrituras)
				ObjectOutputStream ois -> FileOutputStream -> File -> oos.write(new Alumno())

			Cosas importantes -> los objetos propios -> implementar la interfaz Serializable y ademas tener un atributo private final static Long serialVersionUID = 123L;

	json -> intercambiar info -> estructurada en par clave - valor -> Map<Key,Value>
		[] -> JsonArray
		{} -> JsonObject
		{
			name:"Nombre1",
			apellido: "Apellido",
			edad: 41,
			experiencia: false
			caracteristicas:[]
		}
		HttpClient -> HttpRequest -> HttpResponse -> response.body() -> JSON.getJSONArray("caracteristicas")


entender los tipos de variables
dominar las estructuras de control -> como se escriben, que evaluan
entender metodos "base" de las estructuras de almacenamiento -> array, arraylist , hashmap
conceptos de poo -> firma de clases, herencias, interfaces, constructores
conceptos de javafx -> estructura de elementos -> elementos y eventos
base datos -> conexion -> tipos de "ejecutores" ps st rs -> metodos de cada uno 
flujos -> diferenciar entre entrada / salida y que lee o escribe cada flujo




